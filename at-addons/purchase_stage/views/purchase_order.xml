<?xml version="1.0" encoding="UTF-8"?>

<odoo>

    <record id="purchase_order_form_stages" model="ir.ui.view">
        <field name="name">purchase.order.form.stages</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="after">
                <header>
                    <field name="stage_id" widget="statusbar" options="{'clickable': '1'}"/>
                </header>
            </xpath>

            <xpath expr="//sheet/group[1]" position="inside">
                <group>
                    <field name="stage_type"/>
                    <field name="consignment_type"/>
                    <field name="start_date"/>
                    <field name="end_date"/>
                    <field name="delay_days"/>
                    <field name="finished_date"/>
                </group>
            </xpath>

            <xpath expr="//notebook" position="inside">
                <page name="purchase_stage_history" string="Stage History">
                    <field name="stage_history_ids" mode="tree">
                        <tree>
                            <field name="stage_id" widget="badge"/>
                            <field name="start_date"/>
                            <field name="end_date"/>
                            <field name="finished_date"/>
                            <field name="delay_days"/>
                        </tree>
                    </field>
                </page>
            </xpath>
            
        </field>
    </record>

    <record id="purchase_order_kpis_tree_stages" model="ir.ui.view">
        <field name="name">purchase.order.inherit.purchase.order.tree.stages</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_kpis_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='invoice_status']" position="after">
                <field name="stage_id" widget="badge" optional="show"/>
                <field name="start_date" optional="show"/>
                <field name="end_date" optional="show"/>
                <field name="finished_date"/>
            </xpath>
        </field>
    </record>

    <record id="purchase_order_view_tree_stages" model="ir.ui.view">
        <field name="name">purchase.order.view.tree.stages</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_view_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='invoice_status']" position="after">
                <field name="stage_id" widget="badge" optional="show"/>
                <field name="start_date" optional="show"/>
                <field name="end_date" optional="show"/>
            </xpath>
        </field>
    </record>

</odoo>